define([], function () {
  return {
    PropertyPaneDescription: 'Chart tiles are configured from the canvas itself in edit mode',
    BasicGroupName: 'General',
    StudioTitleFieldLabel: 'Studio title'
  };
});
